.box-content
  = simple_form_for @country, html: { class: "basic" } do |f|
    = f.error_notification class: "warning"

    = f.input :name

    .form-group
      %span.no-control-label
      .controls
        = f.submit t('actions.save'), class: "btn btn-primary"
        = link_to t('actions.cancel'), countries_path(@contry), class: "btn btn-default"
