last_row = sheet.rows.size

sheet.add_row(
  [
    'SUMMA:',
    (1..20).map { nil },
    (21..23).map { |i| "=SUM(#{Axlsx.col_ref(i)}2:#{Axlsx.col_ref(i)}#{last_row})" },
    (24..27).map { nil }
  ].flatten
)

(0..27).each do |col|
  sheet.rows.last.cells[col].style = @style.sum
end
