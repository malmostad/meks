.box-content
  = simple_form_for @language, html: { class: "basic" } do |f|
    = f.error_notification class: "warning"

    = f.input :name

    = render 'application/submit', form: f, cancel_path: languages_path
