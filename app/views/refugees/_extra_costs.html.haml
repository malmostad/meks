.refugee_extra_costs
  %h2.group-title Extra kostnader
  - if @refugee.refugee_extra_costs.blank?
    %p Inga extra kostnader är registrerade
  - else
    - @refugee.refugee_extra_costs.each do |extra_cost|
      .extra_cost
        = kv extra_cost, :date
        = kv 'refugee_extra_cost.amount', number_to_currency(extra_cost.amount.to_s, delimiter: ' ')
        = kv extra_cost, :comment

        - if can? :edit, @refugee
          .form-group.action
            %span.no-control-label
            .controls
              = link_to "Redigera extra kostnaden", edit_refugee_refugee_extra_cost_path(@refugee, extra_cost), class: "btn btn-default btn-sm"

  - if can? :edit, @refugee
    = link_to "Ny extra kostnad", new_refugee_refugee_extra_cost_path(@refugee), class: "btn btn-default btn-sm"
