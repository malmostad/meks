.placements
  %h2.group-title Boendeplaceringar
  - if @refugee.placements.empty?
    Har ännu inte placerats.

  - @refugee.placements.each do |a|
    .placement
      = show_attribute('Boende', (link_to a.home.name, home_path(a.home)))
      = show_attribute('Inskriven', a.moved_in_at)
      - if a.moved_out_at.present?
        = show_attribute('Utskriven', a.moved_out_at)
        - if a.moved_out_reason.present?
          = show_attribute('Orsak', a.moved_out_reason.name)
        = show_attribute('Utskrivningskommentar', a.comment)

  .placement= link_to "Ny placering", new_refugee_placement_path(@refugee)
