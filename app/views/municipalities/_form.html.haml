.box-content
  = simple_form_for @municipality, html: { class: 'basic' } do |f|
    = f.error_notification class: 'warning'

    = f.input :name
    = f.input :our_municipality, wrapper: :vertical_radio_and_checkboxes

    = render 'application/submit', form: f, cancel_path: municipalities_path
