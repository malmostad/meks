@workbook.add_worksheet(name: 'Foo') do |sheet|
  sheet.add_row [
    'ID',
    'Namn',
    'Förvald'
  ]
  @legal_codes.find_each do |legal_code|
    sheet.add_row [
      legal_code.id,
      legal_code.name,
      legal_code.pre_selected ? 'Ja' : 'Nej'
    ]
  end
end
