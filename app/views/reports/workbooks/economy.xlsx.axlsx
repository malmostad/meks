@workbook.add_worksheet(name: @first_sheetname) do |sheet|
  render partial: 'reports/workbooks/economy_headings', locals: { sheet: sheet}
  render partial: 'reports/workbooks/economy_rows', locals: { sheet: sheet }
  sheet.column_widths(*(0..sheet.cols.size).map { 18 })
end
