@workbook.add_worksheet(name: @first_sheetname) do |sheet|
  render partial: 'reports/workbooks/homes_headings', locals: { sheet: sheet}
  render partial: 'reports/workbooks/homes_rows', locals: { sheet: sheet }
end
