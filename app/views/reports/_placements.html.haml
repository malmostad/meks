= form_tag reports_placements_path, class: "basic" do
  %h2.group-title Placeringar
  .form-group
    %span.control-label
      %label{for: 'home_id_'} På boende:
    .controls
      = collection_select(:home_id, nil, @homes, :id, :name, { prompt: 'Alla' }, { class: 'form-control chosen' })

  .form-group
    %span.control-label
      %label{for: 'placement_selection'} Urval:
    .controls
      = select_tag('placement_selection', options_for_select("Alla" => 'all', "Överlapppande per barn" => 'overlapping'),
        class: 'form-control chosen')

  .form-group
    %span.control-label
      %label{for: 'placements_from'} Inskrivna mellan:
    .controls
      .input-group.input-daterange
        = text_field_tag 'placements_from', DateTime.now.to_date - 2.weeks, class: 'form-control'
        %span.input-group-addon och
        = text_field_tag 'placements_to', DateTime.now.to_date, class: 'form-control'

  .form-group
    %span.no-control-label
    .controls
      = submit_tag t('actions.download'), name: nil, class: "btn btn-primary"
