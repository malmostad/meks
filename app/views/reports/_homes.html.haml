= form_tag reports_homes_path, class: "basic" do
  %h2.group-title Boenden
  .form-group
    %span.control-label
      %label{for: 'homes_from'} Registrerade mellan:
    .controls
      .input-group.input-daterange
        = text_field_tag 'homes_from', DateTime.now.to_date - 1.year, class: 'form-control'
        %span.input-group-addon och
        = text_field_tag 'homes_to', DateTime.now.to_date, class: 'form-control'

  .form-group
    %span.no-control-label
    .controls
      = submit_tag t('actions.download'), name: nil, class: "btn btn-primary"
