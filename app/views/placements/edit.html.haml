- title "Redigera boendeplacering"
%section.box.index
  %h1.box-title= h1
  .box-content
    = simple_form_for @placement, url: person_placement_path(@person, @placement), html: { class: "basic" } do |f|
      = f.error_notification class: "warning"
      = render 'form', f: f

- if can?(:destroy, Placement)
  %section.box.show.user.danger-zone
    %h1.box-title Riskområde
    .box-content.basic.form-data
      %section.misc
        %p= "Placeringen kommer att raderas permanent. Detta går inte att ångra."
        .delete= button_to 'Radera placeringen permanent', person_placement_path(@person, @placement), method: :delete, data: { confirm: 'Är du säker på att du vill radera placeringen permanent?' }, class: "btn btn-danger btn-default"
