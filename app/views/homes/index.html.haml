- title "Boenden"
%section.box.index
  %h1.box-title= h1
  %table.full.wrap
    %thead
      %tr
        %td{ colspan: 9 }
          .actions= link_to t('actions.new'), new_home_path, class: "btn btn-primary"
      %tr
        %th Namn
        %th Postort
        %th Boendeform
        %th Målgrupp
        %th Ägarform
        %th Platser
        %th

    %tbody
      - @homes.each do |home|
        %tr
          %td= link_to home.name, home
          %td= home.postal_town
          %td= home.type_of_housings.map(&:name).join(', ')
          %td= home.target_groups.map(&:name).join(', ')
          %td= home.owner_types.map(&:name).join(', ')
          %td= home.seats
          %td.delete= link_to t('actions.delete'), home, method: :delete, data: { confirm: 'Är du säker?' }, class: "btn btn-danger btn-sm"

      %tr
        %td{ colspan: 9 }
          .actions= link_to t('actions.new'), new_home_path, class: "btn btn-primary"
