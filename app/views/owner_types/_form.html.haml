.box-content
  = simple_form_for @owner_type , html: { class: "basic" } do |f|
    = f.error_notification class: "warning"

    = f.input :name

    = render 'application/submit', form: f, cancel_path: owner_types_path
