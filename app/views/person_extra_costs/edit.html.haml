- title "Redigera extra kostnaden"
%section.box.index
  %h1.box-title= h1
  .box-content
    = simple_form_for @person_extra_cost, url: person_person_extra_cost_path(@person, @person_extra_cost), html: { class: "basic" } do |f|
      = f.error_notification class: "warning"
      = render 'form', f: f

- if can?(:destroy, PersonExtraCost)
  %section.box.show.user.danger-zone
    %h1.box-title Riskområde
    .box-content.basic.form-data
      %section.misc
        %p= "Extra kostnaden kommer att raderas permanent. Detta går inte att ångra."
        .delete= button_to 'Radera extra kostnaden permanent', person_person_extra_cost_path(@person, @person_extra_cost), method: :delete, data: { confirm: 'Är du säker på att du vill radera placeringen permanent?' }, class: "btn btn-danger btn-default"
